[package]
name = "coop-app"
version = "0.1.0"
edition = "2021"

[dependencies]
coop-domain = { path = "../coop-domain" }
async-trait = "0.1"
tokio = { version = "1", features = ["sync", "macros", "rt"] }
